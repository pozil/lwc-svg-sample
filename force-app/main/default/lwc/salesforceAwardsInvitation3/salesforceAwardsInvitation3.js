import { LightningElement } from 'lwc';
import SVG_BASE_URL from '@salesforce/resourceUrl/mySvgs';

export default class SalesforceAwardsInvitation3 extends LightningElement {
  logoUrl = SVG_BASE_URL + '/logo.svg';
}